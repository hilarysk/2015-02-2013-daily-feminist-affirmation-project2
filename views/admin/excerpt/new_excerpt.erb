<div class="newandupdate"><h1>Add a new excerpt</h1>
<p><%= @fail_message %><%= @fail_message2 %></p>  

<p><strong>1. Please double-check that the excerpt you want to add isn't already in the collection:</strong><br><br>
  First, see if the excerpt's source is in the library; if so, select that source, and hit the button to see existing excerpts.</p>
<p><form action="<%= "#{@path}" %>">
   <select name="source">
     <% @excerpt_sources.each do |source| %>    
       <option value="<%= "#{source}" %>"><%= "#{source}" %></option>
    <% end  %>
  </select><br><br>  
<input type="submit" value="SEE EXCERPTS" class="navbutton" style="width:105px">

  
  <br><br>
</form></p>

<% if @excerpt_choice %> 
  <%= erb :"#{@excerpt_choice}" %>
<% end %>
<br><br>
<hr></hr>
<p><strong>2. If the excerpt you want to add isn't included in the list above, then please fill out each every field in the form below.</strong></p>  
  
<form action="/admin/excerpt/success" method="post">
<strong>Excerpt text:</strong><br><br>
<textarea cols="60" rows="8" maxlength="825" autofocus required name="excerpt" placeholder="(Maximum of 825 characters.)">
</textarea>
<br><br>
<strong>Excerpt source:</strong><br>
  Choose from existing sources:<br><br>                                   
   <select name="source1">
     <% @excerpt_sources.each do |source| %>
       <option value="<%= "#{source}" %>"><%= "#{source}" %></option>
    <% end  %>
  </select><br><br>
  
  Or enter a new source:<br><br>
  <input type="text" name="source" placeholder="Name of the book or magazine."><br><br>
<strong>Author/writer:</strong><br>
(Note: If author or writer is not listed below, you need to <a href="/admin/person?action=new">create a person entry</a> for the author, first.)<br><br>
<select name="person_id">
<% @person_names_ids.each do |object| %>
<option value="<%= "#{object.id}" %>"><%= "#{object.person}" %></option>
<% end  %>
</select><br><br>
<input type="hidden" name="action" value="new">
<input class="navbutton" type="submit" value="SUBMIT">
<br><br>
</form>
  


  
  
  
  </div>
<div class="bottomspacer"></div>
<div class="bottomspacer"></div>